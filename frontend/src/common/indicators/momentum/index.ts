import {
  calculateEMA,
  calculateSMA,
  defaultEMAParams,
  defaultSMAParams,
} from "./movingAverages";
import { calculateKernelRegression, defaultKRParams } from "./kernelRegression";
import { calculateRSI, defaultRSIParams } from "./relativeStrength";

export {
  calculateEMA,
  calculateSMA,
  calculateRSI,
  calculateKernelRegression,
  defaultEMAParams,
  defaultSMAParams,
  defaultRSIParams,
  defaultKRParams,
};
